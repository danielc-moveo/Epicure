export interface NavLinkProps {
  styles: {
    margin: string;
    fontSize: string;
    letterSpacing: string;
  };
}

export interface IconProps {
  src: string;
  isMainLogo: boolean;
  styles: {
    width: string;
    height: string;
    margin: string;
  };
}
